name: Deploy to cPanel FTP

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Build & Deploy via FTP to cPanel
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 🟢 Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🛠️ Build project
        run: npm run build

      - name: 📂 Upload files via FTP to cPanel
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER || secrets.SERVER || vars.FTP_SERVER || vars.SERVER }}
          username: ${{ secrets.FRONTEND_USERNAME || secrets.FTP_USERNAME || secrets.USERNAME || vars.FRONTEND_USERNAME || vars.FTP_USERNAME || vars.USERNAME }}
          password: ${{ secrets.FRONTEND_PASSWORD || secrets.FTP_PASSWORD || secrets.PASSWORD || vars.FRONTEND_PASSWORD || vars.FTP_PASSWORD || vars.PASSWORD }}
          port: ${{ secrets.FTP_PORT || vars.FTP_PORT || 21 }}
          protocol: ${{ secrets.FTP_PROTOCOL || vars.FTP_PROTOCOL || 'ftp' }}
          local-dir: ./dist/
          server-dir: ${{ secrets.REMOTE_DIR || vars.REMOTE_DIR || '/' }}
          dangerous-clean-slate: false
